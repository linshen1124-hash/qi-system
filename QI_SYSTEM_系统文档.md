# QI SYSTEM 后勤管理系统 · 系统设计文档

> 版本：v0.2　|　编制：后勤管理处　|　日期：2026-07-27
> 用途：系统整体规划、**前端设计规范（已定稿）**、功能与数据说明

---

## 一、项目概述

### 1.1 背景
后勤管理处日常工作长期依赖 Excel、Word 等办公软件维持运转：车辆补助按月手工誊算、出入证/车证靠一个表格加一堆文件夹维护、合同与费用到期全凭记忆、同一份数据在多张表之间反复抄录。效率低、易出错、难追溯。

**QI SYSTEM** 旨在把这些"表格驱动"的重复劳动，收敛为一个**统一、轻量、可提醒**的管理系统：一次录入、多处复用，台账带附件，到期自动提醒。

### 1.2 系统定位
- **一个人也能用得顺、多人也能扩展**的内网后勤管理台账系统。
- 以「**台账 + 附件 + 提醒**」为核心，先解决最痛的重复劳动。
- 面向内网部署，**零外部依赖、开箱即用、离线可用**。

### 1.3 设计原则
| 原则 | 含义 |
|---|---|
| 够用即好 | 优先覆盖高频业务，不过度设计 |
| 一次录入 | 数据只录一次，汇总/报表自动生成 |
| 有据可查 | 每条记录可挂扫描件、审批单等附件 |
| 主动提醒 | 到期、缴费、周期事务自动进入提醒中心 |
| 中文为主 | 界面以中文为主，英文仅作视觉点缀 |
| 平滑演进 | 先单机单人，架构预留多人与权限 |

---

## 二、技术架构

### 2.1 选型说明（为内网量身定）
内网服务器通常**无法联网、无法安装第三方软件包**，因此技术栈刻意保持极简、且所有资源本地化：

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3 标准库（`http.server` + `sqlite3`） | **零第三方依赖**，无需 pip 安装 |
| 数据 | SQLite 单文件数据库 | 备份即拷贝文件夹 |
| 前端 | 原生 HTML / CSS / JavaScript | 无需构建、无框架、无 CDN |
| 字体 | 思源黑体 Noto Sans SC + Anton **本地打包**（`static/fonts/`） | **不依赖 Google Fonts，离线可用** |
| 图标 | 内联线条 SVG | 不依赖 emoji / 图标字体，离线可用、风格统一 |
| 附件 | 本地文件系统 + 数据库登记 | 扫描件、审批单就近存储 |

> ⚠️ 说明：设计原型（Stitch 生成）曾使用 Tailwind CDN、Google 字体、远程图标，这些在内网**无法加载**。正式系统已将所需样式与字体全部**本地化内联**，确保断网也能正常显示。

### 2.2 部署与运行
1. 目标机器安装 Python 3。
2. 拷贝整个 `qi-system` 文件夹到内网服务器。
3. 双击 `启动QISYSTEM.bat`（或执行 `python server.py`）。
4. 浏览器访问 `http://本机IP:8080`，科室内多台电脑可同时访问。

### 2.3 目录结构
```
qi-system/
├─ 启动QISYSTEM.bat      启动入口
├─ server.py             后端服务 + 路由 + 业务逻辑
├─ db.py                 数据库表结构
├─ static/
│   ├─ index.html        页面骨架
│   ├─ app.js            前端逻辑（配置驱动的增删改查引擎）
│   ├─ style.css         设计系统落地
│   ├─ fonts/            本地字体（Anton 等）
│   └─ prototypes/       Stitch 定稿原型（风格参考）
├─ data/qi.db            数据库（自动生成）
└─ uploads/              附件存储
```

### 2.4 演进路径
- **单机 → 多人**：已预留 `app_user` 用户表，后续叠加登录与角色权限。
- **SQLite → PostgreSQL**：数据层封装在 `db.py`，用户增多时可平滑迁移。

---

## 三、前端设计规范（已定稿）

> 视觉风格已通过 Stitch 原型定稿：一套 **新粗野主义（Neo-Brutalist）"V2 Bold"** 风格。重要原则：**中文为主，英文仅作点缀**。

### 3.1 整体气质
大胆、醒目、有秩序感。**紫色侧栏 + 橙色主区 + 霓虹绿强调**，配黑色粗描边、块状硬投影、大号标题。信息以白色卡片与表格承载，对比强烈、一眼可辨。

### 3.2 中英文分工（核心原则）
| 层级 | 语言 | 字体 | 用途 |
|---|---|---|---|
| 主标题、正文、表格、导航、按钮 | **中文** | 微软雅黑（加粗） | 承载全部实际信息 |
| 页面小标（kicker）、字段标签、分组名、大号数字 | 英文/数字（点缀） | **Anton / 等宽体** | 仅作视觉修饰，不承载关键信息 |

例：页面顶部小号英文 `OVERVIEW` 之下，是大号中文标题「工作台」。英文即使看不懂也不影响使用。

### 3.3 色彩规范
| 用途 | 颜色 | 色值 |
|---|---|---|
| 侧栏底色 | 紫 | `#6100e1` |
| 侧栏悬停 | 亮紫 | `#7b34ff` |
| 强调面板（提醒中心/表头/弹窗头） | 深紫 | `#7A32FF` |
| 页面主区底色 | 橙 | `#FF9900` |
| 主操作 / 激活项 | 霓虹绿 | `#00FF99` |
| 描边 + 主文字 | 墨黑 | `#161c27` |
| 卡片 / 面板 | 白 | `#ffffff` |
| 次要文字 | 灰 | `#5b6472` |
| 警告 / 临期 | 黄 | `#E0A400` |
| 危险 / 逾期 | 红 | `#E5484D` |

**状态用色约定**：有效/履行中/已缴 = 霓虹绿；临期(≤提醒天数) = 黄；逾期/作废 = 红。全系统统一。

### 3.4 字体规范
- 中文（主）：**思源黑体 Noto Sans SC**（本地打包，含 400/500/700）。与西文 grotesk 同源设计，中英风格统一、观感精致；标题用 700。
- 英文点缀 / 大号数字：`Anton`（本地打包），粗壮、大写。
- 小标签 / 字段名 / kicker：等宽字体 `Consolas`，大写 + 字距放宽。
- 字号：页面标题 32px；卡片数字 44px（Anton）；正文 14px；小标签 11px。

### 3.4.1 图标规范
- 全部使用**线条图标**（描边 SVG，`stroke-width≈1.75`，`currentColor` 随主题变色）。
- **不使用 emoji、不使用图标字体**（离线可靠、风格统一）。
- 图标内联在前端代码中，随页面加载，无网络依赖。

### 3.5 描边、投影、圆角、间距
- **描边**：卡片/面板/侧栏 4px 纯黑（`#161c27`）；表格分隔线 2px。
- **硬投影**（无模糊、实心偏移）：卡片 `6px 6px 0 #161c27`；弹窗 `8px 8px 0`；小控件 `4px 4px 0`。
- **悬停**：元素上移 1–2px、投影加大，形成"立体块"手感。
- **圆角**：卡片/面板 16–22px；按钮/标签 胶囊（999px）；输入框 10px。
- **间距**：内容外边距 30px；卡片间距 20px；卡片内边距 18–20px。

### 3.6 组件规范
| 组件 | 规范 |
|---|---|
| **侧栏导航** | 紫底白字，按业务分组；每项为「线条图标 + 中文（主）+ 英文（副，小号大写）」双行；激活项霓虹绿底、左侧黑粗条 |
| **统计卡片** | 白底黑边硬投影；左上中文小标 + 右上圆形图标徽标；大号 Anton 数字 |
| **数据面板** | 白底黑边卡片，标题栏深色分隔；提醒类用深紫强调面板 |
| **数据表格** | 深紫表头（大写小标签）、斑马纹行、悬停高亮、行末"编辑/删除" |
| **胶囊标签** | 黑边胶囊表示状态；颜色随语义（绿/黄/红） |
| **弹窗表单** | 黑边硬投影模态框，深紫标题栏，双列栅格表单 |
| **提醒行** | 左侧色条（黄/红）+ 中文事项 + 英文类型小标 + 剩余天数 |
| **按钮** | 胶囊 + 黑边 + 硬投影；主按钮霓虹绿；按下时投影收起（按压感） |
| **轻提示 Toast** | 底部居中胶囊，2 秒消失 |

### 3.7 交互约定
- 列表页右上角统一"+ 新增"；每行右侧"编辑 / 删除"。
- 删除有二次确认；保存/删除后 Toast 反馈并自动刷新。
- 日期用日期选择器；引用字段（司机/车辆/合同）用下拉选择。
- 到期日在列表中自动附带"剩N天 / 逾期"胶囊标签。

---

## 四、功能模块

### 4.1 模块总览
| 分组 | 模块 | 状态 |
|---|---|---|
| 总览 | 📊 工作台 | ✅ v0.1 |
| 车辆与司机 | 🛣️ 行车记录 / 🧮 司机补助 / 🧑‍✈️ 司机档案 / 🚗 车辆档案 | ✅ v0.1 |
| 房产与用房 | 🏢 用房分配 / 🪪 出入证·车证 | ✅ v0.1 |
| 合同与费用 | 📄 合同管理 / 💰 费用缴纳 | ✅ v0.1 |
| 事务 | ✅ 待办事项 / ⚙️ 系统设置 | ✅ v0.1 |
| 规划中 | 安全生产 / 房地产(人防) / 规章制度库 / 党建 等 | 🔜 后续 |

### 4.2 核心模块说明

**📊 工作台**
数据总览 + 到期与待办**自动提醒中心**：汇集证件到期、合同到期、合同缴费、费用待缴、待办事项，按时间排序，逾期标红。

**🛣️ 行车记录 → 🧮 司机补助（本系统最大价值点）**
录入行车记录（日期、司机、车辆、部门、路线、起止公里表、使用人、加班），系统**自动计算公里数**（止−起）。到月底，司机补助模块**从行车记录自动汇总**每位司机的总公里与加班小时，按标准自动算出：
> 补助 = 里程 × 里程单价 + 加班小时 × 加班单价 + 其他项

无需再手工誊抄月度公里表与补助单。单价可在系统设置中配置。

**🪪 出入证 / 车证**：统一台账，到期自动提醒，可挂扫描件附件。
**📄 合同 / 💰 费用**：登记到期日与缴费日，进入到期提醒，合同可挂扫描件。
**⚙️ 系统设置**：配置里程单价、加班单价、到期提前提醒天数等参数。

---

## 五、数据模型

| 表 | 中文 | 关键字段 |
|---|---|---|
| `driver` | 司机 | 姓名、电话、在岗 |
| `vehicle` | 车辆 | 车牌、车型、在用 |
| `trip_record` | 行车记录 | 日期、司机、车辆、部门、路线、起止公里、公里数、使用人、加班小时 |
| `subsidy_month` | 月度补助 | 司机、年月、总公里、里程单价、加班时长、加班单价、其他项 |
| `room` | 用房分配 | 院区、楼、房间号、部门、编制人数 |
| `permit` | 出入证/车证 | 类型、编号、持证人、部门、车牌、发放/到期日、状态 |
| `contract` | 合同 | 名称、类别、对方单位、金额、起止日、缴费周期、下次缴费日、状态 |
| `fee_bill` | 费用 | 类别、所属期、金额、应缴日、是否已缴、缴费日 |
| `todo` | 待办 | 事项、截止日、是否完成、归属模块 |
| `attachment` | 附件 | 所属表、所属记录、文件名、大小 |
| `app_user` | 用户（预留） | 用户名、显示名、角色、启用 |
| `setting` | 系统设置 | 键、值 |

---

## 六、路线图

| 阶段 | 内容 |
|---|---|
| **v0.1** | 系统骨架 + 工作台 + 车辆/补助 + 用房/证件 + 合同/费用 + 待办/设置 |
| **v0.2（当前）** | 前端换肤为定稿风格（中文为主、离线可用） |
| **v0.3** | 按真实业务细化字段与规则；历史 Excel 数据一键导入；补助明细导出/打印 |
| **v0.4** | 新增模块：安全生产（隐患台账、检查计划、责任书、培训演练） |
| **v0.5** | 房地产/人防、规章制度库、党建等更多板块 |
| **v1.0** | 多用户登录与权限、操作日志、数据迁移至 PostgreSQL（视人数而定） |

---

## 七、待确认事项（下一步细化）

1. 司机补助真实规则：里程/加班单价、"其他"项含义、"院平台 / 公司平台"两套汇总的区别。
2. 出入证/车证台账需要的完整字段。
3. 历史 Excel 数据的导入范围与优先级。
4. 下一个优先落地的新模块。
5. 视觉：橙色主区饱和度是否需要压低（长时间办公观感）。

---

*本文档随系统迭代持续更新。*

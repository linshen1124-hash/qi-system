# QI SYSTEM · 业务模型与规则体系蓝图

> 版本 v1.0 · 2026-07-28 起草
> 本文件是系统的"宪法级"参考：所有业务模型、规则、演进都对照此文档。它本身是可持续体系的一部分，随规则演进而版本化更新。

---

## 0. 定位与愿景

**一句话定位**：在既有规章制度**之下**，建立一套*可被 AI 执行的操作规则系统（execution-rule system）*。

**问题诊断**：本单位（类政府部门）规章齐备、检查严格、运行平稳。纰漏不来自规则缺失，而来自**执行漂移**——遗忘、人为干扰、不按要求执行。

**系统价值**：成为**唯一事实源**。把"该做什么·何时做·依据什么·留什么证据·谁负责"从纸面固化为数据，消除执行的解释空间与漂移余地。

**三个目标**
1. **留存过去** —— 把历年积累的工作成果梳理、编目、结构化，永久可溯。
2. **适配未来** —— 规则即数据，可随业务与制度变化演进，不改代码即可调规则。
3. **AI 执行** —— 未来操作者是 AI 而非人。AI 在规则边界内高效执行，人只定规则、看异常。

**范式**：过去 → 留存为**记录**；现在 → 固化为**规则**；未来 → 交给 **AI** 在规则下执行。

---

## 1. 目标架构：三层 + 横切

```
┌─────────────────────────────────────────────────────────┐
│  L3 执行层  Execution / AI-operable                        │
│  稳定API契约 · 自省端点(/api/model) · 义务队列 · 审计留痕     │
│  写入校验(越界即拒) · 幂等 · AI 操作手册                     │
├─────────────────────────────────────────────────────────┤
│  L2 规则层  Rules-as-Data                                  │
│  rule 表(触发/条件/义务/期限/依据/证据/责任) → 规则引擎        │
│  → 推导 obligation(义务/待办，带状态+证据+依据)              │
├─────────────────────────────────────────────────────────┤
│  L1 记录层  System of Record                               │
│  台账(25+板块) · 附件 · 档案索引 · 来源溯源(provenance)       │
└─────────────────────────────────────────────────────────┘
   横切：鉴权与角色(app_user) · 审计日志 · 规则版本化 · 备份
```

**现状定位**：L1 已基本建成（约 25 个业务板块）；L2 仅有零散到期提醒（`auto_sync_todos`），未成引擎；L3 尚未开始；横切仅有单机无鉴权。

---

## 2. 业务模型图谱（从现有数据推算）

每个业务域 = **实体**（对应表）+ **生命周期**（状态流）+ **规则**（触发·期限·依据·证据·责任）+ **上报/检查点**。

| # | 业务域 | 核心实体(表) | 生命周期 | 关键规则（触发→义务·期限·证据） | 周期性上报/检查点 |
|---|---|---|---|---|---|
| 1 | **采购** | procurement, contract, supplier | 需求申报→预算(一上)→立项→方式选择→招标→定标→合同→验收→入账/付款 | 超限额须走政府集中/公开招标；单一来源须论证意见表；完成须验收表 | 年度"一上"预算申报；政府采购信息表 |
| 2 | **固定资产** | asset | 采购入账→使用/保管→年度盘点→报废鉴定→处置 | 年度须盘点且账实相符；报废须鉴定审批表；盘盈盘亏须说明表 | 年度盘点上报条保 |
| 3 | **合同/供应商** | contract, fee_bill, supplier, supplier_eval | 签订→履行→周期缴费→到期(续签/结束)；供方入库→评价 | 到期前须提醒续签；缴费按周期；年度须做供应商评价 | 合同台账；年度供方评价 |
| 4 | **车辆/司机** | vehicle, trip_record, subsidy_month, driver | 建档→行车记录→月度补助汇总→年检/报废 | 年检有效期、强制报废期到期须提醒；补助按行车记录月度汇总 | 年度公车运行情况公示 |
| 5 | **证件/门禁** | permit, visitor | 办理→有效期→到期换证/退证；访客报备 | 证件到期前提醒；访客须实名报备 | —— |
| 6 | **房产/用房** | room, housing | 分配/入住→收费→退租；用房登记 | 住房年度缴费；欠费须跟踪 | 用房情况登记 |
| 7 | **节能** | energy_reading, energy_activity | 月度抄表→用量/费用汇总→上报→会议/宣传 | 每月须抄表并汇总；须参加部系统节能会议并报回执 | 月度能耗；年度节能报送 |
| 8 | **工会/福利** | staff, welfare | 名册维护→节日福利发放→签领确认 | 每次福利须签领留痕；子女统筹卡年度统计 | 节日福利；年度统计 |
| 9 | **人事-工勤** | worker | 入岗→在岗管理→离岗 | 工勤补助按月；证件/银行信息须维护（敏感） | 花名册 |
| 10 | **人事-出国** | overseas | 申请→审批→出行→回访→保密提醒 | 因私出国须审批表+保密承诺+回访表 | 逐项审批留痕 |
| 11 | **人事-职称** | title_eval | 申报→评审→通过/未通过 | 年度评审窗口；须评审表+个人总结 | 年度职称评审 |
| 12 | **党群/宣传** | party, publicity, subscription | 学习/组织生活/入党；宣传报送；报刊征订 | 组织生活按周期；宣传须报送；报刊年度征订 | 周期性学习；年度征订 |

> **建模层次**：反映**循环性业务**的进入结构化模型（上表）；**一次性/项目型/个人事务**（如某次宿舍装修、疫情统计、防汛、入党个人材料）只做**档案编目**（见 §5），不建模。

---

## 3. AI 可操作契约（Execution Contract）

让 AI 能**仅凭 API + 本文档**自主执行，需满足：

1. **确定性接口**：资源即表，REST 风格 CRUD（现有）。所有枚举、必填、状态流在模型中声明。
2. **自省端点** `/api/model`：返回全部业务模型——表结构、字段枚举、模块配置、规则清单、义务定义。AI 启动即读，无需硬编码知识。
3. **义务队列** `/api/obligations`：返回"现在/未来应做之事"，每条含：依据(authority)、期限(due)、所需证据(evidence)、责任(role)、当前状态。这是 AI 的"任务源"。
4. **动作留痕** `audit_log`：每次写操作记录 who/what/when/before/after。AI 的一切操作可回溯、可审计——强合规环境的刚需。
5. **写入校验（越界即拒）**：规则在写路径上强制执行。例：超预算采购、缺验收证据即标记完成、非法状态流转 → 服务端拒绝并给出依据。AI 无法绕过规则。
6. **幂等与安全**：写操作支持幂等键，避免重复执行；危险动作（删除/结算）需显式确认位。
7. **AI 交战规则（Rules of Engagement，写入操作手册）**：只在规则内操作；每步留证；遇异常上报不擅自绕过；不推断未授权动作。

---

## 4. 规则模型（Rule-as-Data）

把现有零散的 `auto_sync_todos` 泛化为**通用规则引擎**。

**`rule` 表（规则即数据）**
| 字段 | 含义 |
|---|---|
| domain | 业务域 |
| name | 规则名 |
| trigger_type | 触发类型：日期字段到期 / 状态进入 / 周期(月/季/年) / 缺失检测 |
| target_table, date_field | 作用对象 |
| condition | 生效条件(如 status='有效') |
| lead_days | 提前量 |
| obligation_tmpl | 生成的义务标题模板 |
| evidence_required | 需要的证据类型(附件/字段) |
| authority | 依据(制度名/文号) |
| responsible_role | 责任角色 |
| severity | 严重级(提醒/必办/红线) |
| active, version | 启用/版本 |

**`obligation` 表（引擎产出的义务）**
`rule_id, entity, entity_id, due_date, state(pending/done/overdue/exception/waived), evidence_attachment_id, actor, closed_at`。

**引擎**：定时/触发时扫描 rule × 数据 → 生成/更新 obligation；到期未闭环 → overdue；闭环需满足 evidence_required。义务同时喂给工作台提醒和 AI 义务队列。

---

## 5. 过去数据的留存策略

1. **不可变源**：`qi-bangong/` 原样保留为唯一档案源，不改动。
2. **档案索引** `archive_index`：编目全部 ~2141 文件——路径、业务域、年度、类型、大小、来源，系统内**可检索、可溯源**。这是"梳理并留存"的第一落点。
3. **选择性结构化**：高价值台账（合同台账、资产盘点、供应商名录、能耗、住户名单）逐步导入结构化表，每条带 `source_file` 溯源。
4. **分层留存**：循环业务→结构化；一次性/个人→仅编目。

---

## 6. 可持续机制

- **规则版本化 + 变更日志**：每次规则改动留痕（改了什么、依据、生效时间），可回溯。
- **数据库迁移规范**：新增列走 `MIGRATIONS`（已建立），不破坏旧库。
- **备份**：拷贝整个文件夹；建议加定期结构化导出(Excel/JSON)。
- **鉴权与角色**：启用 `app_user`，操作分权；AI 以受限角色接入。
- **文档即契约**：本文件与 `/api/model` 保持同步，是人与 AI 的共同事实源。

---

## 7. 路线图（分期）

| 阶段 | 目标 | 交付物 | 完成标准 |
|---|---|---|---|
| **P0**（本次） | 立蓝图 | 本文档 + 现状盘点 | 业务模型全推算、三层架构成文 |
| **P1** 记录层收口 | 留存过去 | 档案索引编目 2141 文件 + provenance + 高价值台账导入 | 系统内可检索任意历史文件并溯源 |
| **P2** 规则层 | 固化规则 | rule/obligation 表 + 规则引擎 + 审计日志 | 规则改数据即生效，义务自动推导，操作可审计 |
| **P3** 执行层 | AI 可操作 | /api/model 自省 + 义务队列 + 写入校验 + AI 操作手册 | AI 仅凭 API+文档自主跑完一个业务闭环 |
| **P4** 可持续 | 长期运行 | 鉴权/角色 + 规则版本化 + 备份/导出 | 多角色安全接入，规则可追溯回滚 |

**贯穿性里程碑**：每期结束用同一把尺子检验——*"AI 能否仅凭 API + 本文档，自主完成该业务域一个完整闭环？"*

---

## 附：现有系统板块清单（L1 记录层现状）

总览 · 采购管理 · 资产管理 · 合同供应商(合同/供应商名录/评价/费用) · 车辆与司机(行车/补助/司机/车辆) · 房产与用房(用房/住房/证件/访客) · 节能改造(汇总/台账/宣传) · 工会职工(花名册/福利) · 人事管理(工勤/出国/职称) · 党群宣传(党群/宣传) · 规章制度 · 事务(报刊/待办/设置)
